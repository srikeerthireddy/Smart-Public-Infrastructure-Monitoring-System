// Instrumentation is disabled for now due to TypeScript loading issues in dev mode
// The simulator will start when the app runs in production

export async function register() {
  // No-op for now
}
